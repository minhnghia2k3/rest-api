// // Validate input
// import {Request, Response, NextFunction} from "express";
// import { AnyZodObject } from "zod";
// // Currying technique, return function from another function
// const validate = (schema) => (req:Request,res:Response,next:NextFunction) => {

// }